<% layout("/layouts/boilerplate") %>

<div class="col-10 offset-1 col-lg-8 offset-lg-3 mb-3">
  <h1>Edit Your Listing Review<%=  %></h1>
  <form method="post" class="needs-validation" novalidate action="/listings/<%= listing._id %>/review/<%= review._id %>">
    <div class="mt-3 mb-1">
      <label class="form-label" for="range">Rating</label>
      <fieldset class="starability-slot">
        <input type="radio" id="no-rate" class="input-no-rate" name="review[rating]" value="0" aria-label="No rating." <%= review.rating == 0 ? 'checked' : '' %> />
        <input type="radio" id="first-rate1" name="review[rating]" value="1" <%= review.rating == 1 ? 'checked' : '' %> />
        <label for="first-rate1" title="Terrible">1 star</label>
        <input type="radio" id="first-rate2" name="review[rating]" value="2" <%= review.rating == 2 ? 'checked' : '' %> />
        <label for="first-rate2" title="Not good">2 stars</label>
        <input type="radio" id="first-rate3" name="review[rating]" value="3" <%= review.rating == 3 ? 'checked' : '' %> />
        <label for="first-rate3" title="Average">3 stars</label>
        <input type="radio" id="first-rate4" name="review[rating]" value="4" <%= review.rating == 4 ? 'checked' : '' %> />
        <label for="first-rate4" title="Very good">4 stars</label>
        <input type="radio" id="first-rate5" name="review[rating]" value="5" <%= review.rating == 5 ? 'checked' : '' %> />
        <label for="first-rate5" title="Amazing">5 stars</label>
      </fieldset>      
    </div>
    <div class="mb-3">
      <label class="form-label" for="comment">Comment</label>
      <textarea
        name="review[comment]"
        id="comment"
        rows="5"
        cols="30"
        class="form-control"
        id="comment"
        required
      ><%= review.comment %></textarea>
      <div class="invalid-feedback">Please add comment for review.</div>
    </div>
    <button class="btn btn-outline-dark">Submit</button>
  </form>
</div>
